interface Input {
  base_currency_name: string;
  base_currnecy_code: string;
  convert_currency_name: string;
  convert_currency_code: string;
  base_value: number;
  convert_value: number;
}

export default Input;
